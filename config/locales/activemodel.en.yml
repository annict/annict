en:
  activemodel:
    attributes:
      api/v1/me/slots_index_params:
        fields: fields
        filter_ids: filter_ids
        filter_channel_ids: filter_channel_ids
        filter_work_ids: filter_work_ids
        filter_started_at_gt: filter_started_at_gt
        filter_started_at_lt: filter_started_at_lt
        filter_unwatched: filter_unwatched
        filter_rebroadcast: filter_rebroadcast
        page: page
        per_page: per_page
        sort_id: sort_id
        sort_season: sort_started_at
      api/v1/me/works_index_params:
        fields: fields
        filter_ids: filter_ids
        filter_season: filter_season
        filter_title: filter_title
        filter_status: filter_status
        page: page
        per_page: per_page
        sort_id: sort_id
        sort_season: sort_season
        sort_watchers_count: sort_watchers_count
      api/v1/records_create_params:
        episode_id: episode_id
      api/v1/records_index_params:
        filter_has_record_comment: filter_has_record_comment
      api/v1/statuses_create_params:
        kind: kind
        work_id: work_id
      api/v1/users_index_params:
        fields: fields
        filter_ids: filter_ids
        filter_usernames: filter_usernames
      api/v1/works_index_params:
        fields: fields
        filter_ids: filter_ids
        filter_season: filter_season
        filter_title: filter_title
        page: page
        per_page: per_page
        sort_id: sort_id
        sort_season: sort_season
        sort_watchers_count: sort_watchers_count
      db/cast_rows_form:
        rows: Casts
      db/character_rows_form:
        rows: Characters
      db/episode_rows_form:
        rows: Episodes
      db/organization_rows_form:
        rows: Organizations
      db/person_rows_form:
        rows: People
      db/program_rows_form:
        rows: Programs
      db/series_work_rows_form:
        rows: Series Works
      db/slot_rows_form:
        rows: Slots
      db/staff_rows_form:
        rows: Staffs
      db/trailer_rows_form:
        rows: PVs
    errors:
      forms:
        db/cast_rows_form:
          invalid: 'are invalid: %{value} is not found'
        db/character_rows_form:
          invalid: 'are invalid: %{value} is not found'
        db/slot_rows_form:
          invalid: 'are invalid: %{value} is not found'
          invalid_start_time: 'have invalid start time'
        db/staff_rows_form:
          invalid: 'are invalid: %{value} is not found'
        db/program_rows_form:
          rows: Streaming Links
        db/series_work_rows_form:
          invalid: 'are invalid: %{value} is not found'
        recordable:
          watched_at_cannot_be_in_the_future: can't be in the future
