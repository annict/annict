ja:
  activemodel:
    attributes:
      api/v1/records_create_params:
        episode_id: episode_id
      api/v1/statuses_create_params:
        kind: kind
        work_id: work_id
      api/v1/works_index_params:
        fields: fields
        filter_ids: filter_ids
        filter_season: filter_season
        filter_title: filter_title
        page: page
        per_page: per_page
        sort_id: sort_id
        sort_season: sort_season
        sort_watchers_count: sort_watchers_count
      db/multiple_episodes_form:
        body: エピソード
