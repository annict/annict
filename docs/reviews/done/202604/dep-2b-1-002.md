# コードレビュー: dep-2b-1

## レビュー情報

| 項目                       | 内容                                                                |
| -------------------------- | ------------------------------------------------------------------- |
| レビュー日                 | 2026-04-03                                                          |
| 対象ブランチ               | dep-2b-1                                                            |
| ベースブランチ             | dep-2-6                                                             |
| 作業計画書（指定があれば） | docs/plans/1_doing/usecase-orchestration-refactor.md（タスク 2b-1） |
| 変更ファイル数             | 38 ファイル                                                         |
| 変更行数（実装）           | +210 / -275 行                                                      |
| 変更行数（テスト）         | +240 / -18 行                                                       |

## 参照するガイドライン

- [@CLAUDE.md#レビュー時に参照するガイドライン](/workspace/CLAUDE.md) - ガイドライン一覧
- [@go/CLAUDE.md](/workspace/go/CLAUDE.md) - Go版の開発ガイド
- [@go/docs/architecture-guide.md](/workspace/go/docs/architecture-guide.md) - アーキテクチャガイド
- [@go/docs/security-guide.md](/workspace/go/docs/security-guide.md) - セキュリティガイドライン

## 過去のレビューで対応方針決定済みの問題

`dep-2b-1-001.md` で以下の3件に対応方針が決定済み:

1. FlashManager テストの追加 → `[x]` テストを追加する → **対応済み**（`flash_manager_test.go` が追加された）
2. `SetFlash` シグネチャの変更 → `[x]` 案A: 今回のタスクでシグネチャを変更する → **対応済み**（不要なパラメータと戻り値が除去された）
3. `SetFlash` 呼び出しパターンの統一 → `[x]` 案B: ファイル全体で統一する → **対応済み**（全ファイルで `h.sessionMgr.SetFlash(w, ...)` パターンに統一された）

## 変更ファイル一覧

### 実装ファイル

- [x] `go/internal/session/flash_manager.go`（新規）
- [x] `go/internal/session/flash.go`
- [x] `go/internal/session/session.go`
- [x] `go/internal/usecase/create_session.go`
- [x] `go/internal/usecase/authenticate_by_password.go`
- [x] `go/internal/usecase/complete_sign_up.go`
- [x] `go/internal/usecase/update_password_reset.go`
- [x] `go/internal/handler/sign_in/create.go`
- [x] `go/internal/handler/sign_in/new.go`
- [x] `go/internal/handler/sign_in_code/create.go`
- [x] `go/internal/handler/sign_in_code/show.go`
- [x] `go/internal/handler/sign_in_code/update.go`
- [x] `go/internal/handler/sign_in_password/create.go`
- [x] `go/internal/handler/sign_in_password/new.go`
- [x] `go/internal/handler/sign_up/create.go`
- [x] `go/internal/handler/sign_up/new.go`
- [x] `go/internal/handler/sign_up_code/create.go`
- [x] `go/internal/handler/sign_up_code/new.go`
- [x] `go/internal/handler/sign_up_code/update.go`
- [x] `go/internal/handler/sign_up_username/create.go`
- [x] `go/internal/handler/sign_up_username/new.go`
- [x] `go/internal/handler/password/edit.go`
- [x] `go/internal/handler/password/update.go`
- [x] `go/internal/handler/password_reset/create.go`
- [x] `go/internal/handler/password_reset/new.go`
- [x] `go/internal/handler/popular_work/index.go`
- [x] `go/internal/handler/db_work/create.go`
- [x] `go/internal/handler/db_work/index.go`
- [x] `go/internal/handler/db_work/new.go`
- [ ] `go/internal/handler/supporters/show.go`
- [ ] `go/internal/handler/supporters_checkout/create.go`
- [ ] `go/internal/handler/supporters_portal/create.go`

### テストファイル

- [x] `go/internal/session/flash_manager_test.go`（新規）
- [x] `go/internal/usecase/create_session_test.go`
- [x] `go/internal/usecase/authenticate_by_password_test.go`
- [x] `go/internal/handler/sign_in_code/update_test.go`

### 設定・その他

- [x] `docs/plans/1_doing/usecase-orchestration-refactor.md`
- [x] `docs/reviews/dep-2b-1-001.md`

## ファイルごとのレビュー結果

### `go/internal/handler/supporters_checkout/create.go`: flash タイプに文字列リテラルを使用

**ステータス**: 要確認

**チェックしたガイドライン**:

- [@CLAUDE.md#開発ワークフロー / 既存コードとの一貫性](/workspace/CLAUDE.md) - 既存コードとの一貫性

**問題点・改善提案**:

- **[@CLAUDE.md#開発ワークフロー / 既存コードとの一貫性]**: `redirectWithError` メソッド（L124）で `"error"` という文字列リテラルを使用しているが、他のすべてのハンドラーでは `session.FlashError` 定数を使用している。`supporters_portal/create.go`（L85）も同様。

  今回の差分ではシグネチャ変更に伴う機械的な書き換えが行われたが、既存コードのリテラル使用がそのまま残っている。

  **修正案**:

  ```go
  // supporters_checkout/create.go L124
  h.sessionManager.SetFlash(w, session.FlashError, i18n.T(ctx, messageKey))

  // supporters_portal/create.go L85
  h.sessionManager.SetFlash(w, session.FlashError, i18n.T(ctx, messageKey))
  ```

  **対応方針**:

  <!-- 開発者が回答を記入してください -->
  - [x] 定数に変更する
  - [ ] 現状維持（今回のスコープ外、既存コードの問題）
  - [ ] その他（下の回答欄に記入）

  **回答**:

  ```
  （ここに回答を記入）
  ```

## 設計改善の提案

設計改善の提案はありません。

## 総合評価

**評価**: Approve

**総評**:

前回のレビュー（dep-2b-1-001.md）で指摘された3件がすべて対応済み:

1. **FlashManager のテスト追加**: `flash_manager_test.go` が223行で新規追加され、`SetSuccess`/`SetError`/`GetFlash`/不正Cookie のケースを網羅的にテスト
2. **`SetFlash` シグネチャの簡素化**: `context.Context`、`*http.Request`、戻り値 `error` が除去され、`SetFlash(w, flashType, message)` に変更
3. **呼び出しパターンの統一**: 全ハンドラーで `h.sessionMgr.SetFlash(w, ...)` / `h.sessionManager.SetFlash(w, ...)` パターンに統一

作業計画書タスク 2b-1 の要件もすべて満たされている:

- `CreateSessionUsecase.Execute` から `flashMessage` パラメータが除去された
- `AuthenticateByPasswordUsecase.Execute` から `flashJSON` パラメータが除去された
- Cookie ベースの `FlashManager` が Wikino パターンに合わせて導入された
- 旧 `FlashManager`（`session.Manager` ラッパー）が削除され、新しい `flash_manager.go` に置き換えられた
- 全ハンドラーの `GetFlash` 呼び出しが新シグネチャ `GetFlash(w, r)` に統一された
- ログイン成功時の flash 設定が UseCase から Handler（Presentation 層）に移動された

指摘は文字列リテラル vs 定数の使用の1件のみで、軽微。
