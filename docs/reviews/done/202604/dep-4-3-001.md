# コードレビュー: dep-4-3

## レビュー情報

| 項目                       | 内容                                                               |
| -------------------------- | ------------------------------------------------------------------ |
| レビュー日                 | 2026-04-03                                                         |
| 対象ブランチ               | dep-4-3                                                            |
| ベースブランチ             | dep-4-2                                                            |
| 作業計画書（指定があれば） | docs/plans/1_doing/usecase-orchestration-refactor.md（タスク 4-3） |
| 変更ファイル数             | 7 ファイル                                                         |
| 変更行数（実装）           | +58 / -7 行                                                        |
| 変更行数（テスト）         | +72 / -11 行                                                       |

## 参照するガイドライン

- [@CLAUDE.md#レビュー時に参照するガイドライン](/workspace/CLAUDE.md) - ガイドライン一覧
- [@go/CLAUDE.md](/workspace/go/CLAUDE.md) - Go 版の開発ガイド
- [@go/docs/architecture-guide.md](/workspace/go/docs/architecture-guide.md) - アーキテクチャガイド
- [@go/docs/handler-guide.md](/workspace/go/docs/handler-guide.md) - HTTP ハンドラーガイドライン

## 変更ファイル一覧

### 実装ファイル

- [x] `go/cmd/server/main.go`
- [x] `go/internal/handler/ics/handler.go`
- [x] `go/internal/handler/ics/show.go`
- [x] `go/internal/usecase/get_user_calendar.go`

### テストファイル

- [ ] `go/internal/handler/ics/show_test.go`
- [ ] `go/internal/usecase/get_user_calendar_test.go`

### 設定・その他

- [x] `docs/plans/1_doing/usecase-orchestration-refactor.md`

## ファイルごとのレビュー結果

### `go/internal/usecase/get_user_calendar_test.go`

**ステータス**: 要修正

**チェックしたガイドライン**:

- [@go/CLAUDE.md#テスト戦略](/workspace/go/CLAUDE.md) - テストのベストプラクティス

**問題点・改善提案**:

- **[@go/CLAUDE.md#テストのベストプラクティス]**: `t.Parallel()` が使用されていない

  既存の usecase テスト（例: `authenticate_by_password_test.go`）では `t.Parallel()` が使用されています。トランザクション分離により並行実行は安全なので、一貫性のために追加すべきです。

  ```go
  // 現在のコード（14行目付近）
  t.Run("正常系: ユーザーのカレンダーデータを取得できる", func(t *testing.T) {
      db, tx := testutil.SetupTestDB(t)
  ```

  **修正案**:

  ```go
  t.Run("正常系: ユーザーのカレンダーデータを取得できる", func(t *testing.T) {
      t.Parallel()

      db, tx := testutil.SetupTestDB(t)
  ```

  同様に異常系のテスト（47行目付近）にも `t.Parallel()` を追加してください。

  **対応方針**:
  - [x] 両方のサブテストに `t.Parallel()` を追加する
  - [ ] 現状のまま（理由を回答欄に記入）

  **回答**:

  ```
  （ここに回答を記入）
  ```

### `go/internal/handler/ics/show_test.go`

**ステータス**: 要修正

**チェックしたガイドライン**:

- [@go/CLAUDE.md#テスト戦略](/workspace/go/CLAUDE.md) - テストのベストプラクティス

**問題点・改善提案**:

- **[@go/CLAUDE.md#テストのベストプラクティス]**: `t.Parallel()` が使用されていない

  このファイルの全テスト関数（`TestShow_UserNotFound`, `TestShow_EmptyUsername`, `TestShow_QueryParam` 等）に `t.Parallel()` が追加されていません。これは既存の問題であり本ブランチで導入されたものではありませんが、テストの UseCase 初期化パターンを変更するついでに追加することを検討してください。

  **修正案**:

  各テスト関数の先頭に `t.Parallel()` を追加。

  **対応方針**:
  - [ ] 本ブランチのスコープ外として対応しない
  - [x] ついでに `t.Parallel()` を追加する

  **回答**:

  ```
  （ここに回答を記入）
  ```

## 設計改善の提案

設計改善の提案はありません。

## 総合評価

**評価**: Comment

**総評**:

作業計画書タスク 4-3 の要件（`GetUserCalendarUsecase` の作成、Handler の UseCase 経由化、Repository フィールドの除去、テスト修正）はすべて適切に実装されています。

**良かった点**:

- UseCase の命名規則（`Get{Entity}Usecase`、`{Action}{Entity}Input` / `{Action}{Entity}Output`）が既存パターンと一貫している
- Handler から Repository への直接依存が正しく除去され、UseCase 経由に変更されている
- `show.go` のエラー判定が `err == sql.ErrNoRows` から `errors.Is(err, sql.ErrNoRows)` に改善されている（UseCase がエラーをラップするため必要な変更）
- テストも UseCase 経由に正しく更新されている

**軽微な指摘**:

- UseCase テストに `t.Parallel()` が不足しています（既存 usecase テストとの一貫性）
- Handler テストの `t.Parallel()` 不足は既存の問題のためスコープ外でも可
