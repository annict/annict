# コードレビュー: dep-2b-1

## レビュー情報

| 項目                       | 内容                                                                |
| -------------------------- | ------------------------------------------------------------------- |
| レビュー日                 | 2026-04-02                                                          |
| 対象ブランチ               | dep-2b-1                                                            |
| ベースブランチ             | dep-2-6                                                             |
| 作業計画書（指定があれば） | docs/plans/1_doing/usecase-orchestration-refactor.md（タスク 2b-1） |
| 変更ファイル数             | 29 ファイル                                                         |
| 変更行数（実装）           | +171 / -161 行                                                      |
| 変更行数（テスト）         | +17 / -18 行                                                        |

## 参照するガイドライン

- [@CLAUDE.md#レビュー時に参照するガイドライン](/workspace/CLAUDE.md) - ガイドライン一覧
- [@go/CLAUDE.md](/workspace/go/CLAUDE.md) - Go版の開発ガイド
- [@go/docs/architecture-guide.md](/workspace/go/docs/architecture-guide.md) - アーキテクチャガイド
- [@go/docs/security-guide.md](/workspace/go/docs/security-guide.md) - セキュリティガイドライン

## 変更ファイル一覧

### 実装ファイル

- [x] `go/internal/session/flash_manager.go`（新規）
- [x] `go/internal/session/flash.go`
- [x] `go/internal/session/session.go`
- [x] `go/internal/usecase/create_session.go`
- [x] `go/internal/usecase/authenticate_by_password.go`
- [x] `go/internal/usecase/complete_sign_up.go`
- [x] `go/internal/usecase/update_password_reset.go`
- [x] `go/internal/handler/sign_in/create.go`
- [x] `go/internal/handler/sign_in/new.go`
- [x] `go/internal/handler/sign_in_code/create.go`
- [x] `go/internal/handler/sign_in_code/show.go`
- [x] `go/internal/handler/sign_in_password/create.go`
- [x] `go/internal/handler/sign_in_password/new.go`
- [x] `go/internal/handler/sign_up/create.go`
- [x] `go/internal/handler/sign_up/new.go`
- [x] `go/internal/handler/sign_up_code/new.go`
- [x] `go/internal/handler/sign_up_username/new.go`
- [x] `go/internal/handler/password/edit.go`
- [x] `go/internal/handler/password/update.go`
- [x] `go/internal/handler/password_reset/create.go`
- [x] `go/internal/handler/password_reset/new.go`
- [x] `go/internal/handler/popular_work/index.go`
- [x] `go/internal/handler/db_work/index.go`
- [x] `go/internal/handler/db_work/new.go`
- [x] `go/internal/handler/supporters/show.go`

### テストファイル

- [x] `go/internal/usecase/create_session_test.go`
- [x] `go/internal/usecase/authenticate_by_password_test.go`
- [x] `go/internal/handler/sign_in_code/update_test.go`

### 設定・その他

- [x] `docs/plans/1_doing/usecase-orchestration-refactor.md`

## ファイルごとのレビュー結果

### `go/internal/session/flash_manager.go`: FlashManager のテストが未作成

**ステータス**: 要確認

**チェックしたガイドライン**:

- [@go/CLAUDE.md#テスト戦略](/workspace/go/CLAUDE.md) - テスト戦略
- [@CLAUDE.md#Pull Requestのガイドライン](/workspace/CLAUDE.md) - 実装とテストのセット化

**問題点・改善提案**:

- **[@CLAUDE.md#Pull Requestのガイドライン / 実装とテストのセット化]**: `flash_manager.go` は新規作成された 112 行のファイルだが、対応するテストファイルが存在しない。作業計画書には「テスト修正」と記載されているが、新規ファイルのユニットテストが不足している。`SetSuccess`/`SetError`/`GetFlash`/`clearFlash` のロジック（Base64 エンコード/デコード、Cookie 設定/削除）はテストで検証すべき。

  **修正案**:

  `go/internal/session/flash_manager_test.go` を新規作成し、以下をテスト:
  - `SetSuccess` で Cookie が正しく設定されること（Base64 エンコード済みの JSON）
  - `GetFlash` で Cookie からメッセージを取得でき、取得後に Cookie が削除されること
  - 不正な Cookie 値（Base64 でない、JSON でない）で `GetFlash` が `nil` を返しつつ Cookie を削除すること

  **対応方針**:

  <!-- 開発者が回答を記入してください -->
  - [x] テストを追加する
  - [ ] テストは後続タスクで追加する（理由を回答欄に記入）
  - [ ] その他（下の回答欄に記入）

  **回答**:

  ```
  （ここに回答を記入）
  ```

### `go/internal/session/flash.go`: `Manager.SetFlash` のシグネチャに不要なパラメータが残存

**ステータス**: 要確認

**チェックしたガイドライン**:

- [@go/CLAUDE.md#コーディング規約](/workspace/go/CLAUDE.md) - コーディング規約

**問題点・改善提案**:

- **[@go/CLAUDE.md#コーディング規約]**: `Manager.SetFlash` のシグネチャが `(_ context.Context, w http.ResponseWriter, _ *http.Request, flashType, message string) error` となっており、`context.Context` と `*http.Request` が使用されていない（`_` で無視）。戻り値の `error` も常に `nil` を返す。これは旧 API との後方互換のために残していると思われるが、呼び出し側が全ハンドラーに分散しているため、今回のタスクスコープで一括変更するか、次のタスクで整理するかの判断が必要。

  **修正案**:

  案A: 今回のタスクで `SetFlash` のシグネチャを変更し、不要なパラメータと戻り値を除去する

  ```go
  func (m *Manager) SetFlash(w http.ResponseWriter, flashType, message string) {
      m.flashMgr.setFlash(w, flashType, message)
  }
  ```

  案B: 後続タスクで一括変更する（今回は後方互換のラッパーとして残す）

  **対応方針**:

  <!-- 開発者が回答を記入してください -->
  - [x] 案A: 今回のタスクでシグネチャを変更する
  - [ ] 案B: 後続タスクで一括変更する（現状のまま）
  - [ ] その他（下の回答欄に記入）

  **回答**:

  ```
  （ここに回答を記入）
  ```

### `go/internal/handler/sign_in_code/create.go`: エラー処理の `SetFlash` 呼び出しパターンが不統一

**ステータス**: 要確認

**チェックしたガイドライン**:

- [@go/CLAUDE.md#コーディング規約](/workspace/go/CLAUDE.md) - 既存コードとの一貫性

**問題点・改善提案**:

- **[@CLAUDE.md#開発ワークフロー / 既存コードとの一貫性]**: 同一ファイル内で `SetFlash` の呼び出しパターンが不統一。エラー時の `SetFlash` は `if err := ... SetFlash(...); err != nil { slog.Error(...) }` パターン（L156）を使い、成功時の `SetFlash` は `_ = ...SetFlash(...)` パターン（L167）を使っている。`SetFlash` は今回の変更で常に `nil` を返すようになったため、エラーハンドリングの有無に実質的な差はないが、同一ファイル内でパターンが混在している。

  これは `sign_in_password/create.go`（L34 vs L76）、`sign_up/create.go`（L135 vs 他のエラー箇所）でも同様。

  **修正案**:

  案A: 今回変更した箇所のみ `_ = ...` パターンに統一（変更していない箇所はそのまま）— 現状の方針

  案B: ファイル全体で `_ = ...` パターンに統一する

  **対応方針**:

  <!-- 開発者が回答を記入してください -->
  - [ ] 案A: 変更した箇所のみ新パターン（現状維持）
  - [x] 案B: ファイル全体で統一する
  - [ ] その他（下の回答欄に記入）

  **回答**:

  ```
  （ここに回答を記入）
  ```

## 設計改善の提案

設計改善の提案はありません。

## 総合評価

**評価**: Comment

**総評**:

作業計画書タスク 2b-1 の内容（FlashManager の導入と CreateSessionUsecase からの flash 除去）が正しく実装されている。

**良かった点**:

- `CreateSessionUsecase.Execute` から `flashMessage` パラメータが除去され、UseCase に Presentation 層の関心事が漏れなくなった
- `AuthenticateByPasswordUsecase.Execute` からも `flashJSON` パラメータが除去され、一貫性が向上した
- Cookie ベースの `FlashManager` が Wikino のパターンに合わせて導入された
- 旧 `FlashManager`（`flash.go` 末尾の `session.Manager` ラッパー）が削除され、新しい `flash_manager.go` に置き換えられた
- `session.NewFlashManager(h.sessionMgr)` パターンの呼び出しがすべて除去され、直接 `h.sessionMgr.SetFormErrors(...)` / `h.sessionMgr.SetFlash(...)` に変更された
- 全ハンドラーの `GetFlash` 呼び出しが新シグネチャ `GetFlash(w, r)` に統一された

**指摘事項**:

- `flash_manager.go` のユニットテストが不足（1 件、要確認）
- `Manager.SetFlash` のシグネチャに不要なパラメータが残存（1 件、要確認）
- エラー処理の `SetFlash` 呼び出しパターンが同一ファイル内で不統一（1 件、要確認）

いずれも軽微な指摘であり、機能的な問題やセキュリティ上の懸念はない。
