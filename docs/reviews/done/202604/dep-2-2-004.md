# コードレビュー: dep-2-2

## レビュー情報

| 項目                       | 内容                                                               |
| -------------------------- | ------------------------------------------------------------------ |
| レビュー日                 | 2026-04-01                                                         |
| 対象ブランチ               | dep-2-2                                                            |
| ベースブランチ             | dep-2-1                                                            |
| 作業計画書（指定があれば） | docs/plans/1_doing/usecase-orchestration-refactor.md（タスク 2-2） |
| 変更ファイル数             | 10 ファイル（Go 実装 5 + テスト 4 + 設定 1）                       |
| 変更行数（実装）           | +173 / -128 行                                                     |
| 変更行数（テスト）         | +253 / -29 行                                                      |

## 参照するガイドライン

- [@CLAUDE.md#レビュー時に参照するガイドライン](/workspace/CLAUDE.md) - ガイドライン一覧
- [@go/CLAUDE.md](/workspace/go/CLAUDE.md) - Go 版開発ガイド
- [@go/docs/architecture-guide.md](/workspace/go/docs/architecture-guide.md) - アーキテクチャガイド
- [@go/docs/handler-guide.md](/workspace/go/docs/handler-guide.md) - HTTP ハンドラーガイドライン
- [@go/docs/validation-guide.md](/workspace/go/docs/validation-guide.md) - バリデーションガイド

## 過去のレビューで対応方針決定済みの問題

以下の問題は過去のレビュー（dep-2-2-001〜003）で対応方針が決定済みのため、本レビューでは再指摘しない：

1. **`Execute` の `flashJSON` パラメータが Input 構造体の外にある** → フェーズ 2b-1 で解消予定のため現状維持（001, 002, 003 で決定）
2. **`flashJSON` が Presentation 層の関心事で Application 層に漏れている** → フェーズ 2b-1 で解消予定のため現状維持（001, 002 で決定）
3. **`slog.Error` → `slog.ErrorContext` の修正** → 33-35 行目も含めてすべて修正する（002, 003 で決定）→ **対応完了を確認**
4. **UseCase 専用ユニットテストの追加** → この PR で追加する（003 で決定）→ **対応完了を確認**

## 変更ファイル一覧

### 実装ファイル

- [x] `go/cmd/server/main.go`
- [x] `go/internal/handler/sign_in_password/create.go`
- [x] `go/internal/handler/sign_in_password/handler.go`
- [x] `go/internal/handler/sign_in_password/validator.go`（削除）
- [x] `go/internal/usecase/authenticate_by_password.go`（新規）
- [x] `go/internal/validator/sign_in_password.go`（新規）

### テストファイル

- [x] `go/internal/handler/sign_in_password/create_test.go`
- [x] `go/internal/handler/sign_in_password/new_test.go`
- [x] `go/internal/usecase/authenticate_by_password_test.go`（新規）
- [x] `go/internal/validator/sign_in_password_test.go`（移動＋リネーム）

### 設定・その他

- [x] `docs/plans/1_doing/usecase-orchestration-refactor.md`

## ファイルごとのレビュー結果

問題のあるファイルはありません。全ファイルがガイドラインに準拠しています。

## 設計改善の提案

設計改善の提案はありません。

## 総合評価

**評価**: Approve

**総評**:

作業計画書タスク 2-2 の要件をすべて満たし、過去 3 回のレビュー（dep-2-2-001〜003）で指摘された問題もすべて対応済みの、完成度の高いリファクタリングです。

**良い点**:

- **Handler が薄い Adapter に変更完了**: `handler.go` の依存が `config`, `session.Manager`, `AuthenticateByPasswordUsecase` の 3 つのみに絞られ、`repository` と `validator` への直接依存が完全に除去されている
- **UseCase のオーケストレーション**: `AuthenticateByPasswordUsecase` がバリデーション → ユーザー検索 → NOT NULL 制約チェック → パスワード照合 → セッション作成の一連のフローを正確に統括している
- **Validator の Application 層への配置**: `internal/validator/sign_in_password.go` への移動と `CreateSignInPasswordValidator` の命名が作業計画書の命名テーブルと一致
- **既存パターンとの一貫性**: UseCase テストが `send_sign_in_code_test.go` や `verify_sign_in_code_test.go` と同じ `db, _ := testutil.SetupTestDB(t)` パターンを使用し、セットアップデータをコミットしてから UseCase を実行するパターンに統一
- **slog の全面修正**: 過去レビューで指摘された `slog.Error` → `slog.ErrorContext` が既存行含めて全修正済み（`create.go` の全 slog 呼び出しがコンテキスト付きに統一）
- **テストカバレッジ**: UseCase テスト 4 ケース（正常系、パスワード不一致、ユーザー未存在、バリデーションエラー + ホワイトスペースのみ）、Handler テスト 7 ケース、Validator テスト 4 ケース + I18n メッセージ検証と網羅的
- **DI 配線の正確な更新**: `cmd/server/main.go` で Validator → UseCase → Handler の初期化チェーンが正しく組み立てられている
- **依存関係ルール遵守**: Handler は `usecase` のみ、UseCase は `repository` + `validator`、Validator は `i18n` + `session` のみに依存し、depguard ルールに準拠
