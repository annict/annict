# コードレビュー: dep-2-2

<!--
このテンプレートの使い方:
1. このファイルを `docs/reviews/` ディレクトリにコピー
   - ファイル名: `{ブランチ名}-{3桁連番}.md`
   - 連番は `docs/reviews/` と `docs/reviews/done/` 配下の全ディレクトリから同一ブランチ名のファイルを検索し、既存の最大連番 + 1 を付与する
   - 例: `sign-up-1-1-001.md`（同一ブランチの2回目のレビューなら `sign-up-1-1-002.md`）
2. ブランチ情報を記入
3. 差分を取得して変更ファイル一覧を更新
4. 各ファイルに対してガイドラインに従っているかチェック
5. 問題点や改善提案を記録（問題がないファイルは「ファイルごとのレビュー結果」に記載しない）

**レビュー後のワークフロー**:
1. レビュアーがこのドキュメントを作成
2. 開発者が各問題点の「対応方針」に回答を記入（判断が必要な項目のみ）
3. 開発者が「追記したので対応をお願いします」とレビュアーに伝える
4. レビュアーが回答に基づいて修正を実施
   - @CLAUDE.md のガイドラインに従って修正し、必要であれば @CLAUDE.md からリンクしている他のドキュメントも参照すること
-->

## レビュー情報

| 項目                       | 内容                                                 |
| -------------------------- | ---------------------------------------------------- |
| レビュー日                 | 2026-04-01                                           |
| 対象ブランチ               | dep-2-2                                              |
| ベースブランチ             | dep-2-1                                              |
| 作業計画書（指定があれば） | docs/plans/1_doing/usecase-orchestration-refactor.md |
| 変更ファイル数             | 11 ファイル                                          |
| 変更行数（実装）           | +173 / -128 行                                       |
| 変更行数（テスト）         | +150 / -15 行                                        |

## 参照するガイドライン

<!--
レビュー時に参照するガイドラインドキュメント。
詳細な一覧は @CLAUDE.md の「レビュー時に参照するガイドライン」セクションを参照してください。
-->

- [@CLAUDE.md#レビュー時に参照するガイドライン](/workspace/CLAUDE.md) - ガイドライン一覧
- [@go/CLAUDE.md](/workspace/go/CLAUDE.md) - Go版開発ガイド（ログ出力、ユースケース命名、バリデーション）
- [@go/docs/architecture-guide.md](/workspace/go/docs/architecture-guide.md) - アーキテクチャガイド（依存関係ルール、UseCase パターン）
- [@go/docs/handler-guide.md](/workspace/go/docs/handler-guide.md) - HTTPハンドラーガイドライン
- [@go/docs/validation-guide.md](/workspace/go/docs/validation-guide.md) - バリデーションガイド

## 変更ファイル一覧

### 実装ファイル

- [x] `go/cmd/server/main.go`
- [ ] `go/internal/handler/sign_in_password/create.go`
- [x] `go/internal/handler/sign_in_password/handler.go`
- [x] `go/internal/handler/sign_in_password/validator.go` (削除)
- [ ] `go/internal/usecase/authenticate_by_password.go`
- [x] `go/internal/validator/sign_in_password.go`

### テストファイル

- [x] `go/internal/handler/sign_in_password/create_test.go`
- [x] `go/internal/handler/sign_in_password/new_test.go`
- [x] `go/internal/validator/sign_in_password_test.go`

### 設定・その他

- [x] `docs/plans/1_doing/usecase-orchestration-refactor.md`
- [x] `docs/reviews/dep-2-2-001.md`

## ファイルごとのレビュー結果

<!--
**重要**: 問題のあるファイルのみ記載してください。
問題がないファイルは「変更ファイル一覧」のチェックボックスにチェックを入れるだけで十分です。
-->

### `go/internal/handler/sign_in_password/create.go`

**ステータス**: 要修正

**チェックしたガイドライン**:

- [@go/CLAUDE.md#ログ出力](/workspace/go/CLAUDE.md) - slog のコンテキスト使い分け
- [@go/docs/architecture-guide.md](/workspace/go/docs/architecture-guide.md) - 依存関係ルール

**問題点・改善提案**:

- **[@go/CLAUDE.md#ログ出力]**: 新規追加コード（69行目）で `slog.Error` を使用しているが、`ctx` が利用可能なため `slog.ErrorContext` を使用すべき

  ```go
  // 問題のあるコード（69行目）
  slog.Error("フォームエラーの設定に失敗", "error", err)
  ```

  **修正案**:

  ```go
  slog.ErrorContext(ctx, "フォームエラーの設定に失敗", "error", err)
  ```

  なお、33行目・35行目・48行目の `slog.Error` も同様の問題があるが、これらは既存コード（本PR以前から存在）のため、本PRのスコープ外とする。

  **対応方針**:

  <!-- 開発者が回答を記入してください -->
  - [x] 修正案の通り69行目を `slog.ErrorContext` に変更する
  - [ ] 69行目に加えて、既存の33行目・35行目・48行目も合わせて修正する
  - [ ] その他（下の回答欄に記入）

  **回答**:

  ```
  （ここに回答を記入）
  ```

### `go/internal/usecase/authenticate_by_password.go`

**ステータス**: 要確認

**チェックしたガイドライン**:

- [@go/docs/architecture-guide.md](/workspace/go/docs/architecture-guide.md) - レイヤー間の依存関係
- [@go/CLAUDE.md#ユースケース](/workspace/go/CLAUDE.md) - UseCase の責務と命名規則

**問題点・改善提案**:

- **[@go/docs/architecture-guide.md#レイヤー間の依存関係]**: `Execute` メソッドが `flashJSON string` を第3引数として受け取り、`CreateSessionUsecase.Execute` に渡している。`flashJSON` は Presentation 層の関心事（ユーザー向けメッセージの表示形式）であり、Application 層の UseCase に漏れている

  ```go
  // 問題のあるコード（51行目）
  func (uc *AuthenticateByPasswordUsecase) Execute(ctx context.Context, input AuthenticateByPasswordInput, flashJSON string) (*AuthenticateByPasswordResult, error) {
  ```

  作業計画書のフェーズ 2b-1 で Cookie ベースの flash 管理に移行し、このパラメータが除去される予定であることは確認済み。現時点では `CreateSessionUsecase` が `flashJSON` を要求するため、この構造は一時的に許容される。

  ただし、dep-2-1 で実装された `sign_in/create.go` の Handler は `flashJSON` を UseCase に渡さず、Handler 側で flash 管理を完結させている。同じリファクタリング内で 2 つの異なるパターンが混在している点は留意が必要。

  **対応方針**:

  <!-- 開発者が回答を記入してください -->
  - [x] 現状維持（2b-1 で解消予定のため）
  - [ ] dep-2-1 の sign_in パターンに合わせて、Handler 側で flash 管理を完結させる
  - [ ] その他（下の回答欄に記入）

  **回答**:

  ```
  （ここに回答を記入）
  ```

## 設計改善の提案

設計改善の提案はありません。

## 総合評価

<!--
レビュー全体の総合評価を記述します。

評価基準:
- **Approve**: 問題なし、マージ可能
- **Request Changes**: 修正が必要、修正後に再レビュー
- **Comment**: 軽微な指摘のみ、修正は任意
-->

**評価**: Comment

**総評**:

作業計画書タスク 2-2 の要件を満たす実装になっている。主な変更点は以下の通り：

- `handler/sign_in_password/validator.go` → `validator/sign_in_password.go` への移動と命名変更（`CreateSignInPasswordValidator`）が計画通り完了
- 新規 `AuthenticateByPasswordUsecase` が作成され、バリデーション・ユーザー検索・パスワード照合・セッション作成を統括するオーケストレーターとして機能
- Handler から `repository` と `validator` への直接依存が除去され、UseCase 経由のみに統一
- `cmd/server/main.go` での DI 初期化も正しく更新
- テストが適切に修正・追加されている

**良い点**:

- Handler が薄い Adapter として HTTP 入出力変換のみに専念する設計に変更されている
- `newTestHandler` ヘルパーの導入でテストコードの重複が削減されている
- Validator のテストがテーブル駆動テストで網羅的に書かれている

**指摘事項**:

1. **slog.Error のコンテキスト不足**（要修正）: 新規コードで `ctx` が利用可能にもかかわらず `slog.Error` を使用している箇所がある
2. **flashJSON の UseCase への漏出**（要確認）: Presentation 層の関心事が Application 層に漏れているが、フェーズ 2b-1 で解消予定
