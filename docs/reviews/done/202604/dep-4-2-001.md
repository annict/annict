# コードレビュー: dep-4-2

## レビュー情報

| 項目                       | 内容                                                 |
| -------------------------- | ---------------------------------------------------- |
| レビュー日                 | 2026-04-03                                           |
| 対象ブランチ               | dep-4-2                                              |
| ベースブランチ             | dep-4-1                                              |
| 作業計画書（指定があれば） | docs/plans/1_doing/usecase-orchestration-refactor.md |
| 変更ファイル数             | 7 ファイル                                           |
| 変更行数（実装）           | +70 / -17 行（usecase, handler, main.go）            |
| 変更行数（テスト）         | +38 / -2 行（usecase テスト、handler テスト）        |

## 参照するガイドライン

- [@CLAUDE.md#レビュー時に参照するガイドライン](/workspace/CLAUDE.md) - ガイドライン一覧
- [@go/CLAUDE.md](/workspace/go/CLAUDE.md) - Go 版開発ガイド
- [@go/docs/architecture-guide.md](/workspace/go/docs/architecture-guide.md) - アーキテクチャガイド
- [@go/docs/handler-guide.md](/workspace/go/docs/handler-guide.md) - HTTP ハンドラーガイドライン

## 変更ファイル一覧

### 実装ファイル

- [x] `go/internal/usecase/check_health.go`
- [x] `go/internal/handler/health/handler.go`
- [x] `go/internal/handler/health/show.go`
- [x] `go/cmd/server/main.go`

### テストファイル

- [x] `go/internal/usecase/check_health_test.go`
- [x] `go/internal/handler/health/show_test.go`

### 設定・その他

- [x] `docs/plans/1_doing/usecase-orchestration-refactor.md`

## ファイルごとのレビュー結果

問題のあるファイルはありません。すべてのファイルがガイドラインに準拠しています。

### レビュー詳細（問題なし）

各ファイルについて確認した内容:

**`go/internal/usecase/check_health.go`**:

- 命名規則: `CheckHealthUsecase` / `NewCheckHealthUsecase` / `CheckHealthOutput` / `Execute` メソッド → ガイドライン準拠
- 単一責任: `Execute` メソッドのみ公開 → ガイドライン準拠
- 依存関係: `repository.WorkRepository` のみに依存 → アーキテクチャガイド準拠
- コメント: 日本語で記述 → ガイドライン準拠
- `sql.ErrNoRows` を正常系として扱うロジックが適切（ヘルスチェックは行の存在ではなく DB 接続の確認が目的）
- 既存の `GetPopularWorksUsecase` と完全に同じパターンで実装されており、一貫性がある

**`go/internal/usecase/check_health_test.go`**:

- テスト命名: `TestCheckHealthUsecase_Execute` → ガイドライン準拠
- `testutil.SetupTestDB(t)` を使用 → 既存パターン（`get_popular_works_test.go`）と一致
- サブテストに `t.Run` を使用 → ガイドライン準拠
- 正常系のテストが存在する

**`go/internal/handler/health/handler.go`**:

- Handler 構造体から `workRepo` を除去し、`checkHealthUC *usecase.CheckHealthUsecase` に変更 → 作業計画書の「Handler → Repository 依存を UseCase 経由に統一」に準拠
- `NewHandler` のシグネチャが適切に更新されている

**`go/internal/handler/health/show.go`**:

- UseCase 経由でヘルスチェックを実行 → 作業計画書の目標フローに準拠
- `slog.ErrorContext` を使用 → ログ出力ガイドライン準拠
- エラーレスポンスで詳細なエラー情報をユーザーに漏らしていない → セキュリティガイドライン準拠
- `json.NewEncoder(w).Encode` のエラーハンドリングも適切

**`go/internal/handler/health/show_test.go`**:

- UseCase 経由のハンドラーテストに正しく更新されている
- レスポンスの JSON 構造と各フィールドの検証が適切

**`go/cmd/server/main.go`**:

- `CheckHealthUsecase` の初期化と `healthHandler` への注入が適切
- 依存関係の順序が自然（Repository → UseCase → Handler）

## 設計改善の提案

設計改善の提案はありません。

## 総合評価

**評価**: Approve

**総評**:

タスク 4-2（health の読み取り UseCase 作成）の実装として、作業計画書に記載された仕様通りに正しく実装されています。

- `CheckHealthUsecase` は既存の `GetPopularWorksUsecase` と同じパターンで実装されており、コードベースの一貫性が保たれている
- Handler から Repository への直接依存が適切に除去され、UseCase 経由に統一されている
- テストも正常系を網羅しており、既存テストパターンと一致している
- 変更量も想定範囲内（実装 50 行 + テスト 30 行の想定に対し、実装 70 行 + テスト 38 行）
