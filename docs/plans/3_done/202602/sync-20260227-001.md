# プロジェクト間ファイル同期 2026/02/27

## 概要

Annict、Mewst、Wikino の 3 プロジェクト間でガイドライン・設定ファイルの差分を調査し、統一が必要な項目を特定する。

## タスクリスト

### フェーズ 1: 採用方針の決定

- [x] **1-1**: 以下の差分レポートを確認し、各差分の採用方針を記入する
  - 各差分の「採用方針」チェックボックスにチェックを入れる
  - 必要に応じて「その他」に補足を記入する
  - **重要**: 「スキップ（対応不要）」が選ばれた差分は `.claude/commands/sync.md` の「スキップ済みの差分」セクションに追記するタスクも含めること
  - 「スキップ（あとで対応）」が選ばれた差分は別途作業計画書を作成するタスクを追加すること
  - 他プロジェクトへの反映は、各プロジェクトで `/sync` を実行して対応する
  - **担当**: ユーザー

### フェーズ 2: 差分レポート

#### 差分 1: `.claude/commands/sync.md` の subagent_type と差分レポート形式の違い

**対象ファイル**: `.claude/commands/sync.md`

**差分の内容**:

- **Annict**: Task tool の `subagent_type` に `Explore` を使用。差分レポートをタスクリスト形式（フェーズ 2 以降）で記載。スキップ済み差分の解消確認あり。作業計画書の削除セクションは 6 セクション
- **Mewst**: Task tool の `subagent_type` に `general-purpose` を使用。差分レポートを設計セクション形式で記載。スキップ済み差分の解消確認なし。作業計画書の削除セクションは 3 セクション
- **Wikino**: Annict と同じ形式（`Explore`、タスクリスト形式、解消確認あり、削除 6 セクション）

**採用方針**:

- [x] Annict/Wikino の方式を採用 (Recommended)
- [ ] Mewst の方式を採用
- [x] スキップ（対応不要）
- [ ] スキップ（あとで対応）
- [ ] その他: <!-- 記入 -->

**推奨理由**: Annict/Wikino の形式はタスクリスト形式で採用方針を管理でき、スキップ済み差分のクリーンアップ機能も含まれており、より完全な差分追跡を実現している。Mewst に反映すべき。

---

#### 差分 2: `docs/plans/template.md` の Rails 版ガイドライン参照の違い

**対象ファイル**: `docs/plans/template.md`

**差分の内容**:

- **Annict**: Rails 版ガイドラインとして `@rails/CLAUDE.md`、`@rails/docs/architecture-guide.md`、`@rails/docs/testing-guide.md`、`@rails/docs/security-guide.md` の 4 つを参照
- **Mewst**: `@rails/CLAUDE.md` の 1 つのみ参照
- **Wikino**: Annict と同じ 4 つを参照

**採用方針**:

- [x] Annict/Wikino の方式を採用 (Recommended)
- [ ] Mewst の方式を採用
- [x] スキップ（対応不要）
- [ ] スキップ（あとで対応）
- [ ] その他: <!-- 記入 -->

**推奨理由**: 作業計画書作成時に参照すべきガイドラインを網羅的に列挙している Annict/Wikino の方式が、設計品質の維持に有効。Mewst にドキュメントが存在するかに依存するが、参照リスト自体は統一すべき。

---

#### 差分 3: `rails/eslint.config.mjs` の設定範囲の違い

**対象ファイル**: `rails/eslint.config.mjs`

**差分の内容**:

- **Annict**: `globals.browser`、`globals.node`、`$: "readonly"` を含み、除外パスも `coverage/**/*`、`public/**/*`、`vendor/**/*`、`tmp/**/*`、`log/**/*`、`node_modules/**/*` を追加
- **Mewst**: `globals.browser` のみ。除外パスは `app/assets/builds/**/*` と `tailwind.config.js` の 2 つのみ
- **Wikino**: Mewst と同じ（`globals.browser` のみ、最小限の除外）

**採用方針**:

- [x] Annict の方式を採用 (Recommended)
- [ ] Mewst/Wikino の方式を採用
- [x] スキップ（対応不要）
- [ ] スキップ（あとで対応）
- [ ] その他: <!-- 記入 -->

**推奨理由**: Annict の設定は Rails 固有のディレクトリ（`vendor`、`tmp`、`log`、`public` 等）を明示的に除外しており、不要なリントエラーを防ぐ。`globals.node` は Rake タスクや設定ファイルで Node.js グローバルを使う場合に有用。ただし `$: "readonly"`（jQuery）は不要なプロジェクトもあるため、除外パスの統一のみでもよい。

---

#### 差分 4: `.github/workflows/go-ci.yml` のトリガー条件の違い

**対象ファイル**: `.github/workflows/go-ci.yml`

**差分の内容**:

- **Annict**: `main`、`develop` ブランチへの push と pull_request でトリガー
- **Mewst**: `push` のみ（ブランチ指定なし、PR トリガーなし）
- **Wikino**: Annict と同じ（`main`、`develop` + pull_request）

**採用方針**:

- [x] Annict/Wikino の方式を採用 (Recommended)
- [ ] Mewst の方式を採用
- [x] スキップ（対応不要）
- [ ] スキップ（あとで対応）
- [ ] その他: <!-- 記入 -->

**推奨理由**: PR トリガーを含めることで、マージ前にコード品質を検証できる。ブランチを限定することで不要な CI 実行も抑制できる。

---

#### 差分 5: `.github/workflows/rails-ci.yml` の DB セットアップ方式の違い

**対象ファイル**: `.github/workflows/rails-ci.yml`

**差分の内容**:

- **Annict**: `bin/rails db:setup`（DB 作成 + マイグレーション + シード実行）
- **Mewst**: `bin/rails db:schema:load`（スキーマのみ読み込み）
- **Wikino**: `bin/rails db:schema:load`（スキーマのみ読み込み）

**採用方針**:

- [ ] Annict の方式を採用
- [ ] Mewst/Wikino の方式を採用 (Recommended)
- [x] スキップ（対応不要）
- [ ] スキップ（あとで対応）
- [ ] その他: <!-- 記入 -->

**推奨理由**: CI テストではシードデータは不要なことが多く、`db:schema:load` の方が高速でシンプル。テストに必要なデータは FactoryBot 等で作成するのが一般的。

---

#### 差分 6: `rails/docs/architecture-guide.md` が Mewst に存在しない

**対象ファイル**: `rails/docs/architecture-guide.md`

**差分の内容**:

- **Annict**: 存在する（545 行、詳細なアーキテクチャガイド）
- **Mewst**: 存在しない
- **Wikino**: 存在する（108 行、簡潔な形式）

**採用方針**:

- [ ] Mewst にも作成する (Recommended)
- [x] スキップ（対応不要）
- [ ] スキップ（あとで対応）
- [ ] その他: <!-- 記入 -->

**推奨理由**: Rails 版のアーキテクチャガイドは開発の一貫性を保つために重要。Mewst にも作成すべき。

---

#### 差分 7: `rails/docs/security-guide.md` が Annict・Mewst に存在しない

**対象ファイル**: `rails/docs/security-guide.md`

**差分の内容**:

- **Annict**: 存在しない
- **Mewst**: 存在しない
- **Wikino**: 存在する

**採用方針**:

- [x] Annict・Mewst にも作成する (Recommended)
- [ ] スキップ（対応不要）
- [ ] スキップ（あとで対応）
- [ ] その他: <!-- 記入 -->

**推奨理由**: セキュリティガイドラインは全プロジェクトで必要。Wikino で作成済みの内容をベースに他プロジェクトにも展開すべき。

---

#### 差分 8: `rails/docs/testing-guide.md` が Annict・Mewst に存在しない

**対象ファイル**: `rails/docs/testing-guide.md`

**差分の内容**:

- **Annict**: 存在しない
- **Mewst**: 存在しない
- **Wikino**: 存在する

**採用方針**:

- [x] Annict・Mewst にも作成する (Recommended)
- [ ] スキップ（対応不要）
- [ ] スキップ（あとで対応）
- [ ] その他: <!-- 記入 -->

**推奨理由**: テスト戦略ガイドは開発品質の維持に重要。Wikino で作成済みの内容をベースに他プロジェクトにも展開すべき。

---

#### 差分 9: `CLAUDE.md`（ルート）に Wikino のみ「ドキュメント」セクションが存在

**対象ファイル**: `CLAUDE.md`

**差分の内容**:

- **Annict**: 「ドキュメント」セクションなし
- **Mewst**: 「ドキュメント」セクションなし
- **Wikino**: `## ドキュメント` セクションあり（`docs/specs/` で仕様書を管理する旨を記載）

**採用方針**:

- [x] Wikino の方式を採用し、Annict・Mewst にも追加 (Recommended)
- [ ] スキップ（対応不要）
- [ ] スキップ（あとで対応）
- [ ] その他: <!-- 記入 -->

**推奨理由**: 全プロジェクトで `docs/specs/` を使用した仕様書管理を行っているため、ルートの CLAUDE.md にもその説明を記載すべき。

---

#### 差分 10: `go/CLAUDE.md` に Wikino のみ「ドメイン ID 型」の概念が存在

**対象ファイル**: `go/CLAUDE.md`

**差分の内容**:

- **Annict**: ドメイン ID 型の記述なし
- **Mewst**: ドメイン ID 型の記述なし
- **Wikino**: `type SpaceID string` などの専用ドメイン ID 型を使用するルールを記載。異なるエンティティの ID を取り違える問題をコンパイル時に検出する目的

**採用方針**:

- [x] Wikino の方式を採用し、Annict・Mewst にも追加
- [ ] スキップ（対応不要）
- [ ] スキップ（あとで対応）
- [ ] その他: <!-- 記入 -->

**推奨理由**: 推奨なし（どちらでも良い）。ドメイン ID 型は型安全性を高める手法だが、既存コードの改修コストも考慮が必要。各プロジェクトの状況に応じて判断すべき。

---

#### 差分 11: `go/CLAUDE.md` に Wikino のみ Worker パッケージの記述が存在

**対象ファイル**: `go/CLAUDE.md`

**差分の内容**:

- **Annict**: Worker パッケージの記述なし
- **Mewst**: Worker パッケージの記述なし
- **Wikino**: `internal/worker` パッケージ（river ベースのバックグラウンドジョブキュー）と `internal/email` パッケージの記述あり。Application 層に Worker を追加し、Usecase/Worker/Repository の使い分けを説明

**採用方針**:

- [x] スキップ（対応不要）(Recommended)
- [ ] スキップ（あとで対応）
- [ ] その他: <!-- 記入 -->

**推奨理由**: Worker パッケージは Wikino 固有の機能（バックグラウンドジョブ）に対応するもの。Annict・Mewst で同様の機能が必要になった時点で追加すれば良い。

---

#### 差分 12: `go/.golangci.yml` に Wikino のみ追加レイヤールールが存在

**対象ファイル**: `go/.golangci.yml`

**差分の内容**:

- **Annict**: 標準的な 3 層アーキテクチャのルールのみ
- **Mewst**: Annict と同じ
- **Wikino**: `policy-layer`、`markup-layer`、`worker-layer` の追加ルールあり（Wikino 固有のアーキテクチャ）

**採用方針**:

- [x] スキップ（対応不要）(Recommended)
- [ ] スキップ（あとで対応）
- [ ] その他: <!-- 記入 -->

**推奨理由**: これらは Wikino 固有のアーキテクチャレイヤー（Policy、Markup、Worker）に対応するルール。Annict・Mewst には該当するレイヤーが存在しないため、追加不要。

---

#### 差分 13: `go/docs/security-guide.md` に Wikino のみ「スペース ID によるクエリスコープ」セクションが存在

**対象ファイル**: `go/docs/security-guide.md`

**差分の内容**:

- **Annict**: 該当セクションなし
- **Mewst**: 該当セクションなし
- **Wikino**: 「スペース ID によるクエリスコープ」セクションあり（Wiki のスペースがデータ分離境界のため、`space_id` を WHERE 条件に必ず含めるルール）

**採用方針**:

- [x] スキップ（対応不要）(Recommended)
- [ ] スキップ（あとで対応）
- [ ] その他: <!-- 記入 -->

**推奨理由**: Wikino 固有のドメイン要件（スペース単位のデータ分離）に対応するセクション。Annict・Mewst には該当する要件がないため追加不要。

---

### スキップ済み差分のクリーンアップ結果

以下のスキップ済み差分について確認した結果、**すべて未解消**（全プロジェクトで同一になっていない）のため、クリーンアップ対象なし：

- `rails/CLAUDE.md`: ビジネスロジック層の名称の違い → 未解消
- `Dockerfile.dev`: imagemagick の有無 → 未解消
- `rails/.rspec`: require 対象の違い → 未解消
- `Dockerfile.dev`: Bundler バージョンの違い → 未解消
- `.github/workflows/rails-ci.yml`: RSpec runs-on の違い → 未解消
