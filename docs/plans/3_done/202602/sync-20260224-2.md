# プロジェクト間ファイル同期 2026/02/24 (2回目)

## 設計

### カテゴリ 1: ガイドライン・ドキュメント

プロジェクト固有の差分のみ（Go/Ruby/PostgreSQL バージョン、Wikino の Worker 層・ドメイン ID 型、Annict の画像配信アーキテクチャなど）。同期が必要な差分はなし。

---

### カテゴリ 2: 開発環境

プロジェクト固有の差分が多数（Go/Ruby/Node バージョン、Playwright パッケージ、Redis Tools、Stripe CLI、PostgreSQL クライアントバージョンなど）。同期が必要な差分は以下の 1 件のみ。

#### 差分 1: golangci-lint のバージョン (Dockerfile.dev)

**対象ファイル**: `Dockerfile.dev`

**差分の内容**:

- **Annict**: `v2.6.2`
- **Mewst**: `v2.6.2`
- **Wikino**: `v2.7.2`

**採用方針**:

- [x] Wikino の方式を採用（他プロジェクトに反映）(Recommended)
- [ ] スキップ（対応不要）
- [ ] その他: <!-- 記入 -->

---

### カテゴリ 3: ビルド・CI 設定

プロジェクト固有の差分が多数（PostgreSQL バージョン、Redis サービス、テスト環境変数、RSpec セットアップ方法、ブラウザドライバ、Node.js バージョンなど）。同期が必要な差分は以下の 2 件。

#### 差分 2: go-ci.yml の goimports チェック構造 (.github/workflows/go-ci.yml)

**対象ファイル**: `.github/workflows/go-ci.yml`

**差分の内容**:

- **Annict**: `make templ-generate` + `make goimports` + `git diff` を 1 つのステップで実行
- **Mewst/Wikino**: `make templ-generate` + `git diff` の後に、独立した「Check goimports」ステップで `make goimports-check` を実行

**採用方針**:

- [x] Mewst/Wikino の方式を採用（他プロジェクトに反映）(Recommended)
- [ ] スキップ（対応不要）
- [ ] その他: <!-- 記入 -->

---

#### 差分 3: go-ci.yml のコメント行 (.github/workflows/go-ci.yml)

**対象ファイル**: `.github/workflows/go-ci.yml`

**差分の内容**:

- **Annict**: 各ステップに日本語コメントあり（「依存関係の整合性チェック」「プロジェクトの依存パッケージをダウンロード」「ビルドが成功してバイナリが生成されたことを確認」など）
- **Mewst/Wikino**: コメントなし

**採用方針**:

- [x] Mewst/Wikino の方式を採用（他プロジェクトに反映）(Recommended)
- [ ] スキップ（対応不要）
- [ ] その他: <!-- 記入 -->

---

### カテゴリ 4: ツール設定

差分なし。3 プロジェクト間で完全一致。

---

### カテゴリ 5: Claude Code 設定

#### 差分 4: review.md のレビュードキュメント命名規則 (.claude/commands/review.md)

**対象ファイル**: `.claude/commands/review.md`

**差分の内容**:

- **Annict**: `{ブランチ名}.md` で作成。同名ファイルが既に存在する場合のみ末尾に連番（例: `rate-limit.md` → `rate-limit-1.md`）
- **Mewst/Wikino**: `{ブランチ名}-{3桁連番}.md` で常に連番付き。`docs/reviews/` と `docs/reviews/done/` 配下を検索して連番を決定（例: `sign-up-1-1-001.md`）

**採用方針**:

- [x] Mewst/Wikino の方式を採用（他プロジェクトに反映）(Recommended)
- [ ] スキップ（対応不要）
- [ ] その他: <!-- 記入 -->

---

### カテゴリ 6: ドキュメントテンプレート

#### 差分 5: ハンドラーファイル数の記述 (docs/plans/template.md)

**対象ファイル**: `docs/plans/template.md`

**差分の内容**:

- **Annict**: 「**ファイル名は標準の 8 種類のみ**」
- **Mewst/Wikino**: 「**ファイル名は標準の 9 種類のみ**」

**採用方針**:

- [x] Mewst/Wikino の方式を採用（他プロジェクトに反映）(Recommended)
- [ ] スキップ（対応不要）
- [ ] その他: <!-- 記入 -->

---

#### 差分 6: フェーズ 2・3 のサンプルタスクのプレフィックス (docs/plans/template.md)

**対象ファイル**: `docs/plans/template.md`

**差分の内容**:

- **Annict/Mewst**: フェーズ 2・3 のサンプルタスクにプラットフォームプレフィックス（`[Go]`/`[Rails]`）なし
- **Wikino**: フェーズ 2・3 のサンプルタスクにも `[Go]`/`[Rails]` プレフィックスあり

**採用方針**:

- [x] Wikino の方式を採用（他プロジェクトに反映）(Recommended)
- [ ] スキップ（対応不要）
- [ ] その他: <!-- 記入 -->

---

#### 差分 7: レビュー情報テーブルの「設計書」vs「作業計画書」(docs/reviews/template.md)

**対象ファイル**: `docs/reviews/template.md`

**差分の内容**:

- **Annict**: `| 設計書（指定があれば） | [design-doc-path] |`
- **Mewst/Wikino**: `| 作業計画書（指定があれば） | [plan-doc-path] |`

**採用方針**:

- [x] Mewst/Wikino の方式を採用（他プロジェクトに反映）(Recommended)
- [ ] スキップ（対応不要）
- [ ] その他: <!-- 記入 -->

---

#### 差分 8: 問題点フォーマットの説明文 (docs/reviews/template.md)

**対象ファイル**: `docs/reviews/template.md`

**差分の内容**:

- **Annict**: 場合分け形式（「修正方針が明確な場合」「判断が必要な場合」で説明）
- **Mewst/Wikino**: 番号付きリスト形式（3 つの必須要素: 1. 修正案、2. 対応方針、3. 回答 を明示）

**採用方針**:

- [x] Mewst/Wikino の方式を採用（他プロジェクトに反映）(Recommended)
- [ ] スキップ（対応不要）
- [ ] その他: <!-- 記入 -->

---

#### 差分 9: サンプルファイルの見出し文言 (docs/reviews/template.md)

**対象ファイル**: `docs/reviews/template.md`

**差分の内容**:

- **Annict**: `### path/to/file1.go（例: 修正方針が明確な場合でも「対応方針」「回答」は必須）`
- **Mewst/Wikino**: `### path/to/file1.go（例: 修正方針が明確な場合）`

**採用方針**:

- [ ] Mewst/Wikino の方式を採用（他プロジェクトに反映）(Recommended)
- [x] Annict の方式を採用（他プロジェクトに反映）
- [ ] スキップ（対応不要）
- [ ] その他: <!-- 記入 -->

---

#### 差分 10: サンプルのガイドライン参照先 (docs/reviews/template.md)

**対象ファイル**: `docs/reviews/template.md`

**差分の内容**:

- **Annict**: `[@go/CLAUDE.md](/workspace/go/CLAUDE.md) - コーディング規約`
- **Mewst/Wikino**: `[@CLAUDE.md](/workspace/CLAUDE.md) - コーディング規約`

**採用方針**:

- [x] Mewst/Wikino の方式を採用（他プロジェクトに反映）(Recommended)
- [ ] スキップ（対応不要）
- [ ] その他: <!-- 記入 -->

---

#### 差分 11: 「設計改善の提案」セクションの有無 (docs/reviews/template.md)

**対象ファイル**: `docs/reviews/template.md`

**差分の内容**:

- **Annict**: 「ファイルごとのレビュー結果」の後に直接「総合評価」セクション（「設計改善の提案」セクションなし）
- **Mewst/Wikino**: 「ファイルごとのレビュー結果」と「総合評価」の間に「設計改善の提案」セクションあり（現状・提案・メリット・トレードオフ・対応方針・回答のフォーマット）

**採用方針**:

- [x] Mewst/Wikino の方式を採用（他プロジェクトに反映）(Recommended)
- [ ] スキップ（対応不要）
- [ ] その他: <!-- 記入 -->

---

## タスクリスト

### フェーズ 1: 採用方針の決定

- [x] **1-1**: 差分レポートを確認し、各差分の採用方針を記入する
  - 各差分の「採用方針」チェックボックスにチェックを入れる
  - 必要に応じて「その他」に補足を記入する
  - **担当**: ユーザー

### フェーズ 2: タスクリストの作成

- [x] **2-1**: 採用方針を元に自プロジェクトのタスクリストを追加する
  - ユーザーが記入した採用方針に基づき、フェーズ 3 以降に自プロジェクトの具体的なタスクを追加する
  - **重要**: 「スキップ（対応不要）」が選ばれた差分は `.claude/commands/sync.md` の「スキップ済みの差分」セクションに追記するタスクも含めること
  - 他プロジェクトへの反映は、各プロジェクトで `/sync` を実行して対応する
  - **担当**: Claude

### フェーズ 3: 自プロジェクト (Annict) への反映

- [x] **3-1**: golangci-lint のバージョンを v2.7.2 に更新（差分 1）
  - `Dockerfile.dev`: `v2.6.2` → `v2.7.2`（113 行目）
  - `.github/workflows/go-ci.yml`: `v2.6.2` → `v2.7.2`（44 行目）
  - `go/CLAUDE.md`: `v2.6.2` → `v2.7.2`（234, 241 行目の参考例セクション）
  - **担当**: Claude

- [x] **3-2**: go-ci.yml の goimports チェック構造を変更（差分 2）
  - "Check templ generate" ステップから `make goimports` 行を削除し、`git diff` のチェック対象を `internal/templates/` のみに限定
  - "Check templ generate" と "Check go mod tidy" の間に独立した "Check goimports" ステップを追加（`make goimports-check` を実行）
  - **担当**: Claude

- [x] **3-3**: go-ci.yml のコメント行を削除（差分 3）
  - lint ジョブ: "Check templ generate" のコメント（29 行目）、"Check go mod tidy" のコメント（36-38 行目）を削除
  - test ジョブ: "Get dependencies" のコメント（105 行目）、"Setup test database" のコメント（110-111 行目）を削除
  - build ジョブ: "Build" のコメント（135 行目）、"Check binary exists" のコメント（140-141 行目）を削除
  - **担当**: Claude

- [x] **3-4**: .claude/commands/review.md のレビュードキュメント命名規則を変更（差分 4）
  - 手順 4 のファイル命名規則を変更:
    - 変更前: `docs/reviews/{現在のブランチ名}.md`（同名ファイル既存時のみ連番）
    - 変更後: `docs/reviews/{現在のブランチ名}-{3桁連番}.md`（常に連番付き）
  - `docs/reviews/` と `docs/reviews/done/` 配下を検索して連番を決定する旨を追記
  - **担当**: Claude

- [x] **3-5**: docs/plans/template.md の修正（差分 5, 6）
  - ハンドラーファイル数: 「**ファイル名は標準の 8 種類のみ**」→「**ファイル名は標準の 9 種類のみ**」（97 行目）
  - フェーズ 2・3 のサンプルタスクに `[Go]`/`[Rails]` プレフィックスを追加（200, 204, 210, 215 行目）
  - **担当**: Claude

- [x] **3-6**: docs/reviews/template.md の修正（差分 7, 8, 10, 11）
  - 「設計書（指定があれば）」→「作業計画書（指定があれば）」、`[design-doc-path]` → `[plan-doc-path]`（29 行目）
  - 問題点フォーマットの説明文を番号付きリスト形式に変更（84-91 行目付近）
  - サンプルのガイドライン参照先: `[@go/CLAUDE.md](/workspace/go/CLAUDE.md)` → `[@CLAUDE.md](/workspace/CLAUDE.md)`（102 行目）
  - 「ファイルごとのレビュー結果」と「総合評価」の間に「設計改善の提案」セクションを追加
  - **注**: 差分 9（サンプルファイルの見出し文言）は Annict 方式採用のため、このプロジェクトでは変更不要（他プロジェクトへの反映は各プロジェクトの `/sync` で対応）
  - **担当**: Claude
