- if works.present?
  .works-list
    - works.each do |work|
      - checkin_progress = CheckinProgressService.new(user, work)
      = render 'works/work', work: work, user: user, checkin_progress: checkin_progress

  .text-center
    = paginate works
- else
  - if %w(season recommend works).include?(params[:action]) || (params[:action] == 'search' && params[:q].present?)
    .info.well
      .icon
        i.fa.fa-info-circle
      p = t('works.no_works')
