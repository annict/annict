- title = t "head.title.channel_works.index"
- set_meta_tags(title: title, reverse: true, prefix: false)
- set_page_category "channel_works_index"

= render "channels/subnav"

.c-card.p-1.mt-1.mb-2
  = render "channel_works/work_list", works: @works

= render "application/components/footer"
= render "application/components/tabbar"
