- action_user = notification.action_user
- like = notification.trackable
- review = like.recipient

span.mr-zp25
  = action_user.decorate.name_link
| liked
span.mx-zp25
  = link_to "your review", review_path(review.user.username, review)
| on
span.mx-zp25
  = review.work.decorate.title_link

.small.text-muted
  = local_time_ago_in_words(notification.created_at)
