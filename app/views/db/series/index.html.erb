<% title = t "head.title.db.series.index" %>
<% set_meta_tags(title: title, reverse: true, prefix: false) %>

<div class="container-fluid py-3">
  <div class="mb-3">
    <h1>
      <%= title %>
    </h1>

    <% if policy(Series).create? %>
      <div class="text-end">
        <%= link_to db_new_series_path, class: "btn btn-outline-primary" do %>
          <%= icon "plus" %>
          <%= t "resources._common.new" %>
        <% end %>
      </div>
    <% end %>
  </div>

  <div class="mb-3">
    <%= render "series_list", resources: @series_list %>
  </div>

  <div class="text-center">
    <%= ButtonGroups::PaginationButtonGroupComponent.new(self, collection: @series_list).render %>
  </div>
</div>
