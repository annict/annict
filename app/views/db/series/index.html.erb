<% title = t "head.title.db.series.index" %>
<% set_meta_tags(title: title, reverse: true, prefix: false) %>

<div class="container-fluid py-3">
  <div class="mb-3">
    <h1>
      <%= title %>
    </h1>

    <% if policy(:db_resource).create? %>
      <div class="text-right">
        <%= link_to db_new_series_path, class: "btn btn-outline-primary" do %>
          <%= icon "plus" %>
          <%= t "resources.series.new_series" %>
        <% end %>
      </div>
    <% end %>
  </div>

  <div class="mb-3">
    <%= render "series_list", series_list: @series_list %>
  </div>

  <div class="text-center">
    <%= paginate @series_list %>
  </div>
</div>
