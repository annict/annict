= form_for [:db, person], html: { class: "form-horizontal" } do |f|
  = render "application/v1/error_panel", resource: f.object

  = render "person_fields", f: f

  .form-group
    .col-sm-offset-3.col-sm-9
      = f.submit class: "btn btn-primary"
