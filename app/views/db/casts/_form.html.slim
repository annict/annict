= form_for [:db, work, cast], html: { class: "form-horizontal" } do |f|
  = render "application/v1/error_panel", resource: f.object

  = render "cast_fields", f: f, work: work
    
  .form-group
    .col-sm-offset-3.col-sm-9
      = f.submit class: "btn btn-primary"
