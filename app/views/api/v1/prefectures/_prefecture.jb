# frozen_string_literal: true

json = {}

json[:id] = prefecture.id if params.fields_contain?("#{field_prefix}id")
json[:name] = prefecture.name if params.fields_contain?("#{field_prefix}name")

json
