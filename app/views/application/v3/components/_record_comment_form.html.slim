script#ann-record-comment-form type="x-template"
  textarea name="checkin[body]" :class="{ 'has-new-line': isCommentEditing }" :rows="commentRows" :placeholder="placeholder || ''" v-model="record.comment" @click="expandOnClick(this)" @keyup.enter="expandOnEnter(this)"
