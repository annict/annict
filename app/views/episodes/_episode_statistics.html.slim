- if user_signed_in?
  - user_records = current_user.episode_records.published.where(episode: episode)
td
  .c-episode-row__records-count
    small
      - if user_signed_in?
        = user_records.length
      - else
        = episode.episode_records.published.length
