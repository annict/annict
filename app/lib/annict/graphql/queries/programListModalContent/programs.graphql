query($animeId: ID!) {
  node(id: $animeId) {
    ... on Anime {
      programs(hasSlots: true, onlyViewerCheckedChannels: true) {
        nodes {
          id
          viewerDidCheck
          startedAt

          channel {
            name
          }
        }
      }
    }
  }
}
