query($animeId: ID!) {
  node(id: $animeId) {
    ... on Anime {
      programs(onlyViewerCheckedChannels: true) {
        nodes {
          id
          startedAt

          channel {
            name
          }
        }
      }
    }
  }
}
