query($episodeId: ID!) {
  viewer {
    records(episodeId: $episodeId, orderBy: { field: RATING, direction: DESC }, first: 50) {
      nodes {
        databaseId
        complementableType
        commentHtml
        likesCount
        modifiedAt
        createdAt

        user {
          databaseId
          username
          name
          avatarUrl(size: size200)
          displaySupporterBadge
        }

        trackable {
          ... on Episode {
            databaseId

            anime {
              databaseId
            }
          }
        }

        complementable {
          ... on EpisodeRecord {
            rating
            advancedRating
            commentsCount
          }
        }
      }
    }
  }
}
