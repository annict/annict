<div
  class="align-items-center c-avatar-picture d-flex justify-content-center m-auto <%= @class_name %>"
  style="width: <%= @width %>px;"
>
  <picture>
    <source type="image/webp" srcset="<%= source_srcset("webp") %>">
    <source type="image/jpeg" srcset="<%= source_srcset("jpg") %>">
    <img
      alt="<%= @alt %>"
      class="img-thumbnail rounded-circle"
      loading="lazy"
      src="<%= helpers.ann_avatar_image_url(@user, width: @width, format: "jpg") %>"
    >
  </picture>
</div>
